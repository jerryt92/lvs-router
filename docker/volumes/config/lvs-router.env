KEEPALIVED_CONF="${KEEPALIVED_CONF:-/opt/lvs-router/keepalived/lb3/keepalived.conf}"
HEALTHY_HTTP_PORT="${HEALTHY_HTTP_PORT:-45555}"
ROUTER_HTTP_DOCROOT="${ROUTER_HTTP_DOCROOT:-/opt/lvs-router/run/router-http}"
IPVS_MODULES="${IPVS_MODULES:-ip_vs ip_vs_rr ip_vs_wrr ip_vs_sh}"
LB_RS_TOPOLOGY="${LB_RS_TOPOLOGY:-merged}"
